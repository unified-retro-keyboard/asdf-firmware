# Python tooling for the ASDF firmware repo (Sphinx docs, linters and the
# pinned clang-format that format.sh runs).
# Managed by uv: `uv sync` builds .venv; `uv run <cmd>` runs inside it.
# The key matrix generator (src/asdf_keymap_gen.py) carries its own inline
# script metadata and does not use this environment.

[project]
name = "asdf-tools"
version = "0"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
docs = [
    "sphinx",
    "sphinx-autodoc-typehints",
    "sphinx-rtd-theme",
    "toml",
]
format = [
    "clang-format==21.1.8",
]
dev = [
    "black",
    "flake8",
    "pylint",
    "mypy",
]

[tool.uv]
package = false
default-groups = ["docs", "dev"]
